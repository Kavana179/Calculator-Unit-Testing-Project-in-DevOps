name: SonarCloud Analysis

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build and Analyze
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code from the repository
      - uses: actions/checkout@v4

      # Step 2: Set up Python 3.9
      - name: Set up Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: 3.9

      # Step 3: Install OpenJDK 17
      - name: Install OpenJDK 17
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-17-jdk

      # Step 4: Set Java 17 as JAVA_HOME
      - name: Set Java 17 as JAVA_HOME
        run: |
          echo "JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64" >> $GITHUB_ENV
          java -version  # Verify the correct version is being used

      # Step 5: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest coverage

      # Step 6: Run tests and generate coverage report
      - name: Run tests and generate coverage report
        run: |
          pytest tests/
          coverage run -m pytest
          coverage xml

      # Step 7: Run SonarCloud analysis with debug logging enabled
      - name: SonarCloud Scan with Debug Logging
        uses: SonarSource/sonarcloud-github-action@v1.5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}  # Ensure the token is set in GitHub secrets
        with:
          args: |
            -X  
            -Dsonar.projectKey=Kavana179_Calculator-Unit-Testing-Project-in-DevOps
            -Dsonar.organization=kavana179
            -Dsonar.sources=.
            -Dsonar.language=python
            -Dsonar.python.version=3.9
            -Dsonar.python.coverage.reportPaths=coverage.xml
